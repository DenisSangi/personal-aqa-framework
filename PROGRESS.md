# PROGRESS.md — журнал прогресса `personal-aqa-framework`

**Проект:** UI+API AQA фреймворк с нуля, зеркалящий `java-selenide-pp`. Открыт по решению 2026-07-22 (см. память `framework-build-routine`).
**Формат:** запись на каждую сессию — что сделано, что верно/слабо, что перенесено.

---

## Сессия 1 — 2026-07-23 (началась автоматически, Денис подключился вживую)

**Тема:** Первая сессия проекта — подготовка Tech Lead'ом, затем живое обсуждение и утверждение `PLAN.md` с Денисом.

**Статус:** ✅ План согласован по крупным блокам; цикл задач ещё НЕ начат (следующий шаг — Задача P0-T1)

**Контекст:** сессия запущена по расписанию в 10:00, но это неинтерактивный (автоматический) прогон — Дениса нет на связи, чтобы отвечать на вопросы, обсуждать задачу или писать код. Рабочий цикл проекта по своей природе требует живого диалога («Ты даёшь задачу → обсуждаем → я пишу код → ты ревьюишь» — см. `STARTING-PROMPT.md`), поэтому вместо старта Задачи 1 сессия использована на подготовительную работу, которую Tech Lead может сделать самостоятельно.

**Сделано:**
- Прочитаны все опорные материалы: `Framework explanation/framework-layer-definition.md`, `design-patterns.md` (все 14 паттернов), `DatabaseConnectionUtil.java`, начало `ClinCardMergeCardholderSuccessfullyBaseClass.java` (конвенция `ParticipantState`/balance-констант), память `denis-learning-style.md` и `qa-interview-prep.md`.
- Создан скелет бухгалтерии проекта: `README.md`, `PLAN.md` (14 фаз, ~20 задач верхнего уровня со схемой id/зависимость/мотивировка/теория/тег), `curriculum/README.md` (схема файла-урока + пустой индекс), `theory/README.md` (пустой индекс), `sessions/session-2026-07-23.md`.
- Открытые решения (целевой сайт / БД-подход / тест-раннер) приняты **провизорно** по рекомендованным умолчаниям из `STARTING-PROMPT.md` (automationexercise.com, локальный Postgres в Docker, TestNG) — см. пометку в `PLAN.md`. Это НЕ финальное решение Дениса.
- Код фреймворка **не писался** — сознательно, по правилу «никогда не пишу продакшн-код за Дениса»; Задача 1 (скелет репозитория) требует, чтобы её писал он сам после обсуждения.

**Живое продолжение (тот же день):** Денис подключился, прошлись по `PLAN.md`:
- Денис указал точный пробел: в плане не было отдельной фазы для написания реальных тестов (только единичные демо-тесты внутри инфраструктурных задач). Добавлена **Фаза 10 — Test Coverage** (регистрация/логин, поиск+корзина, API CRUD — 2–3 сценария каждая), Allure/параллелизация (теперь Фазы 11–12) сдвинуты и теперь явно зависят от неё.
- Подтверждены все 3 открытых решения: сайт `automationexercise.com`, БД — локальный PostgreSQL в Docker (с явной оговоркой в P9-T3: самостоятельное упражнение на своей схеме, не сквозной сценарий — публичный сайт доступа к БД не даёт), раннер — TestNG.
- Денис предложил идею для будущего веб-приложения (Цель 3 проекта): построить собственный учебный веб-сервис со сложной микросервисной архитектурой, публичным API и доступной схемой БД, чтобы подход UI→API→Database работал по-настоящему сквозным образом (без компромисса, на который пришлось пойти в решении по БД здесь). Зафиксировано в `PLAN.md` и `STARTING-PROMPT.md`.

**Задача P0-T1 дана и ЗАКРЫТА (аппрув 2026-07-23).** Создан `curriculum/P0-T1-repo-maven-skeleton.md` (полная схема-урок) и `theory/maven-basics.md` (GAV, dependency vs plugin, scope, lifecycle, структура папок, дополнен по запросу Дениса разделом «Структура pom.xml — блок за блоком»). Денис подтвердил формат урока без вопросов — назвал полным соответствием ожиданиям от того, как хочет строить работу над проектом.

Ход работы: по пути словил и разрулил сам IDE-факап (Maven-модуль создался внутри `.idea/` вместо корня — поправил сам, ничего не было потеряно). Написал `pom.xml` (свои GAV-координаты, Java 17, Selenide+TestNG+Allure+Lombok+Jsoup), реальную структуру `src/main`/`src/test`, содержательный placeholder-тест (`Placeholder.countSum` + positive/negative кейсы, не просто заглушка). На ревью нашлась одна реальная ошибка — перепутал `allure-java-commons` (общий модуль) с `allure-testng` (адаптер под свой раннер) — поправил сам после объяснения разницы. Финальный `mvn clean test` — 2/2 зелёно (прогнано через IntelliJ, CLI `mvn` в окружении недоступен). Git-репозиторий инициализирован, публичный GitHub-репо `github.com/DenisSangi/personal-aqa-framework` создан и запушен.

Обе ошибки (IDE content root, allure-java-commons vs allure-testng) записаны в раздел «Типичные ошибки» `P0-T1-repo-maven-skeleton.md` — первый реальный контент для будущего веб-приложения.

**Сильные стороны сессии:** быстро схватывает объяснение через изолированный пример (GAV, структура pom.xml) и сразу применяет к своей задаче; не боится честно сказать «не понял» и спросить прицельно, а не гадать; сам заметил и почистил мелочи (`final` на неизменяемом поле) без подсказки.

**Что перенесено на следующую сессию:**
- Дать Задачу P1-T1 (Config facade + порядок разрешения параметров) — следующая по `PLAN.md`, если Денис готов продолжать в этой же сессии.

**Слабые места / риски:** нет данных — цикл ревью кода ещё не начинался.

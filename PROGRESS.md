# PROGRESS.md — журнал прогресса `personal-aqa-framework`

**Проект:** UI+API AQA фреймворк с нуля, зеркалящий `java-selenide-pp`. Открыт по решению 2026-07-22 (см. память `framework-build-routine`).
**Формат:** запись на каждую сессию — что сделано, что верно/слабо, что перенесено.

---

## Сессия 1 — 2026-07-23 (началась автоматически, Денис подключился вживую)

**Тема:** Первая сессия проекта — подготовка Tech Lead'ом, затем живое обсуждение и утверждение `PLAN.md` с Денисом.

**Статус:** ✅ План согласован по крупным блокам; цикл задач ещё НЕ начат (следующий шаг — Задача P0-T1)

**Контекст:** сессия запущена по расписанию в 10:00, но это неинтерактивный (автоматический) прогон — Дениса нет на связи, чтобы отвечать на вопросы, обсуждать задачу или писать код. Рабочий цикл проекта по своей природе требует живого диалога («Ты даёшь задачу → обсуждаем → я пишу код → ты ревьюишь» — см. `STARTING-PROMPT.md`), поэтому вместо старта Задачи 1 сессия использована на подготовительную работу, которую Tech Lead может сделать самостоятельно.

**Сделано:**
- Прочитаны все опорные материалы: `Framework explanation/framework-layer-definition.md`, `design-patterns.md` (все 14 паттернов), `DatabaseConnectionUtil.java`, начало `ClinCardMergeCardholderSuccessfullyBaseClass.java` (конвенция `ParticipantState`/balance-констант), память `denis-learning-style.md` и `qa-interview-prep.md`.
- Создан скелет бухгалтерии проекта: `README.md`, `PLAN.md` (14 фаз, ~20 задач верхнего уровня со схемой id/зависимость/мотивировка/теория/тег), `curriculum/README.md` (схема файла-урока + пустой индекс), `theory/README.md` (пустой индекс), `sessions/session-2026-07-23.md`.
- Открытые решения (целевой сайт / БД-подход / тест-раннер) приняты **провизорно** по рекомендованным умолчаниям из `STARTING-PROMPT.md` (automationexercise.com, локальный Postgres в Docker, TestNG) — см. пометку в `PLAN.md`. Это НЕ финальное решение Дениса.
- Код фреймворка **не писался** — сознательно, по правилу «никогда не пишу продакшн-код за Дениса»; Задача 1 (скелет репозитория) требует, чтобы её писал он сам после обсуждения.

**Живое продолжение (тот же день):** Денис подключился, прошлись по `PLAN.md`:
- Денис указал точный пробел: в плане не было отдельной фазы для написания реальных тестов (только единичные демо-тесты внутри инфраструктурных задач). Добавлена **Фаза 10 — Test Coverage** (регистрация/логин, поиск+корзина, API CRUD — 2–3 сценария каждая), Allure/параллелизация (теперь Фазы 11–12) сдвинуты и теперь явно зависят от неё.
- Подтверждены все 3 открытых решения: сайт `automationexercise.com`, БД — локальный PostgreSQL в Docker (с явной оговоркой в P9-T3: самостоятельное упражнение на своей схеме, не сквозной сценарий — публичный сайт доступа к БД не даёт), раннер — TestNG.
- Денис предложил идею для будущего веб-приложения (Цель 3 проекта): построить собственный учебный веб-сервис со сложной микросервисной архитектурой, публичным API и доступной схемой БД, чтобы подход UI→API→Database работал по-настоящему сквозным образом (без компромисса, на который пришлось пойти в решении по БД здесь). Зафиксировано в `PLAN.md` и `STARTING-PROMPT.md`.

**Задача P0-T1 дана и ЗАКРЫТА (аппрув 2026-07-23).** Создан `curriculum/P0-T1-repo-maven-skeleton.md` (полная схема-урок) и `theory/maven-basics.md` (GAV, dependency vs plugin, scope, lifecycle, структура папок, дополнен по запросу Дениса разделом «Структура pom.xml — блок за блоком»). Денис подтвердил формат урока без вопросов — назвал полным соответствием ожиданиям от того, как хочет строить работу над проектом.

Ход работы: по пути словил и разрулил сам IDE-факап (Maven-модуль создался внутри `.idea/` вместо корня — поправил сам, ничего не было потеряно). Написал `pom.xml` (свои GAV-координаты, Java 17, Selenide+TestNG+Allure+Lombok+Jsoup), реальную структуру `src/main`/`src/test`, содержательный placeholder-тест (`Placeholder.countSum` + positive/negative кейсы, не просто заглушка). На ревью нашлась одна реальная ошибка — перепутал `allure-java-commons` (общий модуль) с `allure-testng` (адаптер под свой раннер) — поправил сам после объяснения разницы. Финальный `mvn clean test` — 2/2 зелёно (прогнано через IntelliJ, CLI `mvn` в окружении недоступен). Git-репозиторий инициализирован, публичный GitHub-репо `github.com/DenisSangi/personal-aqa-framework` создан и запушен.

Обе ошибки (IDE content root, allure-java-commons vs allure-testng) записаны в раздел «Типичные ошибки» `P0-T1-repo-maven-skeleton.md` — первый реальный контент для будущего веб-приложения.

**Сильные стороны сессии:** быстро схватывает объяснение через изолированный пример (GAV, структура pom.xml) и сразу применяет к своей задаче; не боится честно сказать «не понял» и спросить прицельно, а не гадать; сам заметил и почистил мелочи (`final` на неизменяемом поле) без подсказки.

**Задача P1-T1 дана в тот же день (2026-07-23, 12:33), но запись об этом не попала в этот файл сразу** — исправлено задним числом в Сессии 2. Создан `curriculum/P1-T1-config-facade.md` (Config Facade + порядок разрешения параметров: env var → system property → properties-файл) и `theory/config-resolution.md`. Код Денисом ещё НЕ написан на момент конца Сессии 1 (`src/main` содержит только `Placeholder.java` из P0-T1).

**Что перенесено на следующую сессию:**
- Продолжить с Задачей P1-T1: обсуждение с Денисом (если будут вопросы) → он пишет `FrameworkConfig` сам → ревью → аппрув.

**Слабые места / риски:** нет данных — цикл ревью кода ещё не начинался.

---

## Сессия 2 — 2026-07-24 (автоматический прогон, Денис не на связи)

**Тема:** Плановый ежедневный прогон в 10:00. Как и в Сессии 1, это неинтерактивный запуск — Денис не подключился, обсуждать задачу и ревьюить код некому и не с кем.

**Статус:** Задача P1-T1 остаётся открытой, в том же состоянии, что и в конце Сессии 1 — лесон дан, код не написан.

**Проверено (то, что Tech Lead может сделать в одиночку):**
- Сверил `curriculum/P1-T1-config-facade.md` с реальным кодом-прообразом `gp-core/src/main/java/config/GPConfig.java` — описание метода `getValue(key)` (цепочка env → system property → properties, `IllegalStateException` при отсутствии значения везде) в уроке точно соответствует реальной реализации. Расхождений не найдено, править нечего.
- Обнаружено и исправлено расхождение в бухгалтерии проекта: запись Сессии 1 в этом файле была написана ДО того, как Денис подключился живьём и Задача P1-T1 была фактически дана (файлы `curriculum/P1-T1-config-facade.md` и `theory/config-resolution.md` датированы 12:33, запись в `PROGRESS.md` — 12:27) — из-за этого файл утверждал, что P1-T1 «ещё предстоит дать», хотя она уже дана. Запись выше исправлена.
- `git status` — рабочее дерево чистое, новых коммитов после `1c0a9cd Task P0-T1 is Done` нет. Код по P1-T1 не начат.

**Не сделано (сознательно):** код `FrameworkConfig` не писался и не обсуждался по существу — некому обсуждать и некому писать. Ждём Дениса вживую.

**Живое продолжение (тот же день):** Денис подключился и прошёл P1-T1 от начала до конца.

**Задача P1-T1 дана повторно с нуля** — Денис признался, что первую версию собрал, адаптируя `GPConfig.java` "на глаз" и по подсказкам, без сформированного понимания. Прошли последовательно: (1) объяснение "что/зачем/цена отказа фасада" на верхнем уровне; (2) разбор ловушки IDE "New Resource Bundle" (фича под i18n) вместо обычного файла ресурса; (3) содержимое `.properties`; (4) порядок статической инициализации (`static`-блок должен по тексту стоять раньше полей, которые от него зависят) — с изолированным вакуум-примером; (5) сам смысл трёх уровней (env var / system property / properties-файл) — что каждый такое, чем отличается радиус действия, зачем нужен именно такой порядок приоритета — Денис сам правильно ответил на проверочный вопрос ("для разового локального переопределения — system property").

**Ревью кода — несколько раундов, каждый со свежим багом:**
1. Первая версия `getValue()` растеряла 2 из 3 веток (осталась только проверка env var) — класс не мог загрузиться без переменной окружения (`ExceptionInInitializerError`, подтверждено живым прогоном через `javac`/`java` вне Maven).
2. Заодно — сломанный импорт `sun.net.NetProperties.get` (IDE подставила по совпадению имени метода) и `APP_URL` без инициализации (не компилировалось).
3. После восстановления всех трёх веток — реально прогнано 3 сценария (`java` без флагов / с `-DBASE_URL=...` / с env var) — подтверждено, что приоритет реально работает (env var > system property > файл).
4. Try-with-resources не был try-with-resources — ресурс объявлен вне скобок `try`, поток не закрывался. Поправлено.
5. **DoD выполнен полностью, задача P1-T1 аппрувед.**

**Бонус сверх DoD — `FrameworkConfigSmokeTest`:** Денис сам предложил реально проверить `FrameworkConfig.APP_URL` в работе. Использовал REST Assured (сам добавил зависимость в `pom.xml`, с версией через property — по образцу остальных зависимостей). Несколько раундов ревью и здесь: негативный тест на статус-код сначала был логически бессмысленным (`assertNotEquals(204, ...)`, затем `assertNotEquals(500, ...)` — оба случая логически поглощены позитивным `assertEquals(200, ...)`, реальной информации не добавляли); заменён на содержательно другую проверку — тело ответа. В процессе всплыл реальный баг: `.body().toString()` вместо `.body().asString()` — возвращал `ClassName@hashcode`, а не HTML, тест был бы зелёным всегда. Первая версия content-проверки была завязана на изменчивые данные каталога (`/brand_products/Polo`) — заменена на стабильный структурный маркер (`"All Products"`). Финал: `mvn clean test` — 4/4 зелёных, реальный HTTP-запрос подтверждён живой трассировкой.

Полный список из 8 найденных ошибок с разбором — в `curriculum/P1-T1-config-facade.md`, раздел «Типичные ошибки».

**Сильные стороны:** после каждого замечания — самостоятельно находил правильное направление фикса, не просто менял число/строку наугад (например, при слабом негативном тесте дважды пробовал сузить условие, а на третий раз сам пришёл к content-проверке — качественно другому подходу). Честно признался в непонимании, когда оно было ("плохо понял задачу", "не понял проблему 1") — не гадал, не делал вид.

**Слабые места / риски:** склонен на первой попытке копировать форму решения (из референса или из моей же подсказки) не до конца осмысляя, зачем каждая часть нужна — несколько раз ловил баги именно на этом (провалы веток в `getValue`, поверхностное "негативный тест = сузить число"). Стоит на будущих задачах явно просить его сначала проговорить план словами, до кода.

**Что перенесено на следующую сессию:** Фаза 2 (`RandomGenerators`, `DateTimeUtil`) — следующая по `PLAN.md`. Спросить Дениса, готов ли продолжать в этой же сессии или на следующей.
